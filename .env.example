# ========================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ========================================

# Escolha UMA das opções abaixo:

# OPÇÃO 1: MongoDB Atlas (Cloud)
# Descomente a linha abaixo e configure com suas credenciais do MongoDB Atlas
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&appName=<YourAppName>

# OPÇÃO 2: MongoDB Local via Docker
# Use esta configuração quando rodar com docker-compose
# O hostname 'mongodb' referencia o serviço definido no docker-compose.yml
MONGO_URI=mongodb://admin:admin@mongodb:27017

# OPÇÃO 3: MongoDB Local (rodando fora do Docker)
# Use esta configuração quando rodar a API localmente (sem Docker) mas com MongoDB local
# MONGO_URI=mongodb://admin:admin@localhost:27017

# ========================================
# AMBIENTE
# ========================================
# dev: usa o banco 'parking_db_test'
# prod: usa o banco 'parking_db'
ENV=dev